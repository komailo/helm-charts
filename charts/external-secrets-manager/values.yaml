globals:
  secretStoreRefKind: ClusterSecretStore
  secretStoreRefName: aws-ssm-parameter-store-default
  refreshInterval: 24h

SimpleSecrets:
  # - name: dns-token
  #   dataSecretKey: token
  #   remoteRefKey: /blah/external-dns/dns-token
  #   type: kubernetes.io/dockerconfigjson # optional Kubernetes Secret type (omitted if not defined)

SubPathSecrets:
  # - name: backup-secrets
  #   remoteRefKey: /k8s-dev/backup
  #   type: kubernetes.io/dockerconfigjson # optional Kubernetes Secret type (omitted if not defined)

JsonSecrets:
  # - name: app-env
  #   remoteRefKey: /k8s/app/envfile
  #   namespace: my-namespace
  #   targetName: app-env
  #   type: Opaque # optional Kubernetes Secret type (omitted if not defined)
  #   # remote value must be a JSON object string (keys become Secret data entries)
