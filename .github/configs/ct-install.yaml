## Reference: https://github.com/helm/chart-testing/blob/main/doc/ct_lint-and-install.md
# Don't add the 'debug' attribute, otherwise the workflow won't work anymore
# Only Used for the CT Install Stage
chart-repos: []
exclude-deprecated: true
excluded-charts:
  - cloudflare-tunnel-remote
github-groups: true
target-branch: main
